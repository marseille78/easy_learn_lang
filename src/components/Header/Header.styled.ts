import styled from "styled-components";

export const Root = styled.header`
  background-color: var(--c-navy);
  color: var(--c-white);
  padding: 8px var(--p-container);
  display: flex;
  align-items: center;
  margin-bottom: 24px;
`;
